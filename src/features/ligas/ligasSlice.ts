import { createSlice } from '@reduxjs/toolkit'


export const ligasSlice = createSlice({
  name: 'counter',
  initialState: [],
  reducers: {
  }
})

// Action creators are generated for each case reducer function

export default ligasSlice.reducer